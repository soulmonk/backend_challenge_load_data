'use strict'

class Loader {
  constructor (repositories, opts) {
    this.repositories = repositories
    this.opts = opts
  }

  async proceed () {
    // validate
    // load in db
    // create email list
    // show errors
  }
}

module.exports = Loader
